// Flutter imports:
/* import 'package:flutter/material.dart';

// Package imports:
import 'package:get/get.dart';

// Project imports:
import 'core/app_texts.dart';

class NoDataWidget extends StatelessWidget {
  final String asset;
  final String name;
  final String description;
  const NoDataWidget(this.name, this.description, this.asset);
  @override
  Widget build(BuildContext context) {
    return Center(
      child: ListView(
        shrinkWrap: true,
        physics: const ClampingScrollPhysics(),
        children: [
          Container(
            margin: const EdgeInsets.only(top: 20),
            child: Stack(
              children: [
                Column(
                  children: [
                    Container(
                      margin: const EdgeInsets.symmetric(vertical: 40),
                      height: 150,
                      child: Image.asset(asset),
                    ),
                    Container(
                      margin: const EdgeInsets.symmetric(
                        horizontal: 15,
                        vertical: 10,
                      ),
                      child: Column(
                        children: [
                          NoDataTitleLabel(
                            name,
                          ),
                          // verticalSpaceMedium,
                          Center(
                            child: NoDataDescription(
                              description,
                            ),
                          )
                        ],
                      ),
                    ),
                  ],
                ),
              ],
            ),
          ),
        ],
      ).marginOnly(bottom: 80),
    );
  }
}
 */